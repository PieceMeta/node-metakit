const
  data = require('./data'),
  io = require('./io'),
  messaging = require('./messaging'),
  processors = require('./processors'),
  services = require('./services')

module.exports = {
  data,
  io,
  messaging,
  processors,
  services
}
